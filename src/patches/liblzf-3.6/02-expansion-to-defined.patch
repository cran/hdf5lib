diff -ru src/liblzf-orig/lzfP.h src/liblzf-3.6/lzfP.h
--- src/liblzf-orig/lzfP.h	2010-06-01 08:16:09.000000000 +0000
+++ src/liblzf-3.6/lzfP.h	2026-03-17 23:47:14.588275900 +0000
@@ -79,7 +79,11 @@
  * Unconditionally aligning does not cost very much, so do it if unsure
  */
 #ifndef STRICT_ALIGN
-# define STRICT_ALIGN !(defined(__i386) || defined (__amd64))
+# if defined(__i386) || defined(__amd64)
+#  define STRICT_ALIGN 0
+# else
+#  define STRICT_ALIGN 1
+# endif
 #endif
 
 /*
@@ -142,7 +146,11 @@
 
 #ifndef LZF_USE_OFFSETS
 # if defined (WIN32)
-#  define LZF_USE_OFFSETS defined(_M_X64)
+#  if defined(_M_X64)
+#   define LZF_USE_OFFSETS 1
+#  else
+#   define LZF_USE_OFFSETS 0
+#  endif
 # else
 #  if __cplusplus > 199711L
 #   include <cstdint>
